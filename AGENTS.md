# Agent notes

Release version consistency (package metadata, README current-stable declaration, and install pins) is enforced by `scripts/release.py` during release preparation. See README “Development and releases”.
